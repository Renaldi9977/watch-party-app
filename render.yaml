services:
  - type: web
    name: watch-party
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 10000
